module.exports = {
    presets: [
      '@babel/preset-env', // Mevcut ise
      '@babel/preset-react' // React için JSX desteği
    ],
    plugins: [
      // Diğer eklentiler burada olabilir
    ],
  };
  